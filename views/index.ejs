<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Social App</title>
  </head>
  <script src="https://cdn.tailwindcss.com"></script>
  <body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
      <h3 class="text-3xl mb-5 flex">Create Account</h3>
      <a class="text-sm text-blue-500" href="/login">Login Page</a>
      <form action="/register" method="post">
        <input
          class="px-1 py-2 mt-5 outline-none bg-transparent border-2 border-zinc-800 rounded-md"
          type="text"
          placeholder="username"
          name="username"
        />
        <input
          class="px-1 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md"
          type="text"
          placeholder="name"
          name="name"
        />
        <input
          class="px-1 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md"
          type="number"
          placeholder="age"
          name="age"
        />
        <input
          class="px-1 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md"
          type="email"
          placeholder="email"
          name="email"
        />
        <input
          class="px-1 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md"
          type="password"
          placeholder="password"
          name="password"
        />
        <input
          class="px-5 py-2 ml-3 outline-none bg-blue-500 border-2 border-zinc-800 rounded-md"
          type="submit"
          value="Create Account"
        />
      </form>
    </div>
  </body>
</html>
